import { ShowEvent } from '../types';

/**
 * Dados iniciais da aplicação.
 * Zerado para permitir que o baterista cadastre seus próprios cantores, bandas e shows.
 */
export const INITIAL_SHOWS: ShowEvent[] = [];

